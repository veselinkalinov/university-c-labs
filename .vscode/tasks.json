{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build Active File with Make",
            "command": "C:\\msys64\\usr\\bin\\make.exe",
            "args": [
                "${relativeFileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build",
            "detail": "Uses the generic Makefile to build the current file."
        },
        {
            "type": "cmake",
            "label": "CMake: configure",
            "command": "configure",
            "problemMatcher": [],
            "detail": "CMake template configure task",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
